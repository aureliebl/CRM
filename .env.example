# Database provider for app storage stores
# - sqlite: current local file database (default)
# - postgres: use PostgreSQL for migrated stores
DB_PROVIDER=sqlite

# Required when DB_PROVIDER=postgres
DATABASE_URL=postgres://user:password@localhost:5432/costotest

# Optional: set true on managed PG providers requiring SSL
PGSSL=false
